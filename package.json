{
  "name": "jsdx-monorepo",
  "private": true,
  "engines": {
    "node": ">=8.0.0",
    "yarn": ">=1.5.0"
  },
  "scripts": {
    "format": "prettier --write 'packages/**/*.{js,md}' './*.{js,md}'"
  },
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged"
    }
  },
  "lint-staged": {
    "*.{js,md}": [
      "prettier --write",
      "git add"
    ]
  },
  "devDependencies": {
    "husky": "^3.1.0",
    "lerna": "^3.19.0",
    "lint-staged": "^9.5.0",
    "pre-commit": "1.2.2",
    "prettier": "^1.19.1"
  },
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  }
}
