{
  "name": "jsdx-monorepo",
  "private": true,
  "engines": {
    "node": ">=10.0.0",
    "yarn": ">=1.5.0"
  },
  "scripts": {
    "format": "prettier --write 'packages/**/*.{js,md}' './*.{js,md}'"
  },
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged"
    }
  },
  "lint-staged": {
    "*.{js,md}": [
      "prettier --write"
    ]
  },
  "devDependencies": {
    "husky": "^4.2.3",
    "lint-staged": "^10.0.8",
    "pre-commit": "1.2.2",
    "prettier": "^1.19.1"
  },
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  }
}
